trX = int(input("Введите координату X для клада: "))
trY = int(input("Введите координату Y для клада: "))
X, Y = 0, 0  # точки начала
dirX, dirY = 0, 1
move = 0
dir = [[0, 1, "Север"], [1, 0, "Восток"], [0, -1, "Юг"], [-1, 0, "Запад"]]  # Функция  с аргументом которая должна вернуть список допустимых атрибутов для указанного объекта.
dirpoint = 0
act = str(input('Введите действие: '))
while act != 'стоп': # повторяет команды до тех пор пока не подит написанно стоп
    if act not in ['вперёд', 'налево', 'направо', 'разворот', 'стоп']:  # условия которые выполняются для нохождение клада. То есть каждые развароты
        act = str(input('Введите действие: '))
        continue
    if act == 'вперёд':
        step = int(input("Введите количество шагов: "))
        X, Y = X + dirX * step, Y + dirY * step
        move += 1  # шаги они или прибовляются или уменьшаются 
        act = str(input('Введите действие: '))
        continue
    elif act == 'налево':
        dirpoint -= 1
    elif act == 'направо':
        dirpoint += 1
    elif act == 'разворот':
        dirpoint += 2
    dirX, dirY = dir[dirpoint % 4][0], dir[dirpoint % 4][1]
    move += 1
    act = str(input('Введите действие: '))
print(F"Количество совершённых ходов: {move}\nНаше направление: {dir[dirpoint][2]}")

input("Задание №2 ")
d = int(input())
m = int(input())
g = int(input())
y = g % 100  # номер в столетии
c = g // 100  # количество столетий
if m <= 2:  # разница в месяцах
    m = +10
else:
    m -= 2
if m > 2:
y = y
else:
    y -= 1
pd = d + ((13 * m - 1) // 5) + y + (y // 4 + c // 4 - 2 * c + 777)
day = pd % 7  # высчитывание дней
print(day)

input("Задание №3 ")

list1 = (str(input()))
print(list1[2:3:1])  # 1 выведит третий символ
print(list1[-2])  # 2 Во второй строке выведите предпоследний символ этой строки
print(list1[0:5:1])  # 3 В третьей строке выведите первые пять символов этой строки
print(list1[0:-2:1])  # 4 В четвертой строке выведите всю строку, кроме последних двух символов.
print(list1[0:999:2])  # 5 В пятой строке выведите все символы с четными индексами (считая, что индексация начинается с 0, поэтому символы выводятся начиная с первого).
print(list1[1:999:2])  # 6 В шестой строке выведите все символы с нечетными индексами, то есть начиная со второго символа строки.
print(list1[::-1])  # 7 В седьмой строке выведите все символы в обратном порядке
print(list1[-1::-2])  # 8 В восьмой строке выведите все символы строки через один в обратном порядке, начиная с последнего.
print(len(list1))  # 9 В девятой строке выведите длину данной строки.